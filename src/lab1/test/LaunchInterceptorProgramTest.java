package lab1.test;

import org.junit.jupiter.api.Test;
import lab1.*;

public class LaunchInterceptorProgramTest {
    /**
     * Test the hole program using the "testInputDecisionIsTrue.json" which is
     * structured in a way that should result in the final decision being true.
     */
    @Test
    void decide_ValidInput_FinalDecisionTrue() {
        // The testInputDecisionIsTrue.json file includes points four points on a unit
        // circle and a point in the origin: (0, 0), (1, 0), (-1, 0), (0, 1), (0, -1)
    }
}
